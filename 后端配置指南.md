# 后端配置指南

根据项目结构，这是一个 **Next.js + Supabase** 的项目，后端配置主要在以下几个地方：

## 🎯 后端配置位置

### 1. **环境变量配置（最重要）**
**文件：`.env.local`**

这是前端连接后端的配置文件：

```env
# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
```

### 2. **Supabase 配置**
**文件：`supabase/config.toml`**

这是 Supabase 本地开发的配置文件，包含：
- API 端口：54321
- 数据库端口：54322
- Studio 端口：54323
- 认证配置
- 存储配置
- 实时订阅配置

### 3. **Docker 配置**
**文件：`docker-compose.yml`**

这是 Docker 容器的配置，包含：
- PostgreSQL 数据库
- Supabase Auth（认证服务）
- Supabase REST API
- Supabase Storage（存储服务）
- Kong 网关
- Supabase Studio（管理界面）

### 4. **数据库迁移**
**目录：`supabase/migrations/`**

数据库表结构和初始化 SQL 文件。

### 5. **API 网关配置**
**文件：`kong.yml`**

Kong 网关的路由配置。

---

## 🔧 如何修改后端配置

### 修改数据库连接
编辑 `.env.local`：
```env
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321  # API 地址
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的密钥            # 匿名访问密钥
```

### 修改端口
编辑 `supabase/config.toml`：
```toml
[api]
port = 54321  # API 端口

[db]
port = 54322  # 数据库端口

[studio]
port = 54323  # Studio 管理界面端口
```

### 修改数据库密码
编辑 `docker-compose.yml`：
```yaml
environment:
  POSTGRES_PASSWORD: 你的新密码
```

### 修改认证配置
编辑 `supabase/config.toml`：
```toml
[auth]
site_url = "http://127.0.0.1:3000"  # 前端地址
enable_signup = true                 # 是否允许注册
jwt_expiry = 3600                    # Token 过期时间（秒）
```

---

## 📝 常见配置场景

### 场景 1：切换到云端 Supabase
修改 `.env.local`：
```env
NEXT_PUBLIC_SUPABASE_URL=https://你的项目.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=从Dashboard获取的密钥
```

### 场景 2：修改数据库表结构
在 `supabase/migrations/` 目录创建新的 SQL 文件，然后运行：
```bash
supabase db reset
```

### 场景 3：配置邮件服务
编辑 `supabase/config.toml`：
```toml
[auth.email.smtp]
enabled = true
host = "smtp.gmail.com"
port = 587
user = "your-email@gmail.com"
pass = "env(SMTP_PASSWORD)"
```

---

## 🚀 启动后端服务

### 使用 Docker Compose（推荐）
```bash
# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 停止服务
docker-compose down
```

### 使用 Supabase CLI
```bash
# 启动本地开发环境
supabase start

# 查看服务状态
supabase status

# 停止服务
supabase stop
```

---

## 🔍 验证配置

### 检查服务是否运行
```bash
# 检查 Docker 容器
docker ps

# 检查端口占用
netstat -an | findstr "54321\|54322\|54323"
```

### 访问管理界面
- **Supabase Studio**: http://localhost:54323
- **API 文档**: http://localhost:54321/rest/v1/
- **数据库**: localhost:54322

### 测试 API 连接
```bash
# 测试 API 是否可访问
curl http://localhost:54321/rest/v1/projects
```

---

## 🐛 常见问题

### 问题 1：端口冲突
**解决方案：**
- 修改 `supabase/config.toml` 中的端口配置
- 或者停止占用端口的其他服务

### 问题 2：数据库连接失败
**解决方案：**
- 检查 Docker 容器是否正常运行
- 验证数据库密码是否正确
- 确保防火墙没有阻止端口

### 问题 3：认证失败
**解决方案：**
- 检查 JWT 密钥是否正确
- 确保 Auth 服务正常运行
- 验证 `.env.local` 配置

---

## 📚 相关文档

- [前端配置指南.md](./前端配置指南.md) - 前端 Supabase 客户端配置
- [SETUP_LOCAL.md](./SETUP_LOCAL.md) - 本地开发环境配置
- [QUICK_START.md](./QUICK_START.md) - 快速启动指南
- [README.md](./README.md) - 项目说明

---

## 💡 提示

- 修改配置后需要重启相关服务才能生效
- 生产环境建议使用云端 Supabase 服务
- 定期备份数据库数据
- 保护好 JWT 密钥和数据库密码