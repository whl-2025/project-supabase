# 向量搜索测试示例

## 📋 测试步骤

### 步骤1：配置模型

1. 进入 **向量搜索 → 模型配置**
2. 输入 OpenAI API Key
3. 选择嵌入模型：`text-embedding-3-small`
4. 设置相似度阈值：`0.7`
5. 设置最大结果数：`10`
6. 设置分块大小：`500`
7. 点击 **"保存配置"**

### 步骤2：添加测试数据

进入 **向量搜索 → 数据管理**，添加以下测试数据：

#### 示例数据1：技术文档
- **标题**：React 组件开发
- **内容**：React 是一个用于构建用户界面的 JavaScript 库。组件是 React 应用的基本构建块，可以封装 UI 逻辑和状态。函数组件是 React 推荐的方式，使用 Hooks 可以管理状态和副作用。JSX 语法允许在 JavaScript 中编写类似 HTML 的代码，使组件开发更加直观。
- **分类**：前端开发

#### 示例数据2：技术文档
- **标题**：Vue.js 框架介绍
- **内容**：Vue.js 是一个渐进式 JavaScript 框架，用于构建用户界面。Vue 的核心特性包括响应式数据绑定、组件系统、模板语法等。Vue 3 引入了 Composition API，提供了更灵活的组件组织方式。单文件组件（SFC）将模板、脚本和样式组合在一起，提高了开发效率。
- **分类**：前端开发

#### 示例数据3：产品说明
- **标题**：项目管理工具功能
- **内容**：我们的项目管理工具提供了完整的项目生命周期管理功能。包括项目创建、任务分配、进度跟踪、团队协作等功能。支持看板视图、列表视图、甘特图等多种视图方式。可以设置任务优先级、截止日期、依赖关系等。还提供了丰富的报告和分析功能，帮助团队更好地管理项目。
- **分类**：产品功能

#### 示例数据4：产品说明
- **内容**：任务管理是项目管理的核心功能之一。用户可以创建任务、分配负责人、设置优先级和截止日期。任务支持子任务、标签、附件等功能。可以通过筛选、排序、搜索等方式快速找到需要的任务。任务状态可以设置为待办、进行中、已完成等。系统会自动跟踪任务的进度和完成情况。
- **分类**：产品功能

#### 示例数据5：技术文档
- **标题**：数据库设计原则
- **内容**：数据库设计是应用开发的重要环节。良好的数据库设计应该遵循规范化原则，减少数据冗余，保证数据一致性。主键用于唯一标识记录，外键用于建立表之间的关联关系。索引可以提高查询性能，但也会增加存储空间和更新成本。需要考虑数据的完整性约束，包括实体完整性、参照完整性、用户定义完整性等。
- **分类**：后端开发

#### 示例数据6：技术文档
- **标题**：RESTful API 设计
- **内容**：RESTful API 是一种基于 HTTP 协议的 API 设计风格。使用标准的 HTTP 方法（GET、POST、PUT、DELETE）来表示不同的操作。资源通过 URL 来标识，使用 JSON 格式传输数据。状态码用于表示请求的结果，如 200 表示成功，404 表示资源不存在，500 表示服务器错误。API 应该具有良好的版本控制和文档。
- **分类**：后端开发

### 步骤3：向量化数据

1. 在数据项列表中，依次点击每个数据项的 **"向量化"** 按钮
2. 等待向量化完成（每个数据项需要几秒钟）
3. 确认所有数据项都已向量化（显示"已向量化"标签）

### 步骤4：测试语义搜索

进入 **向量搜索 → 语义搜索**，测试以下查询：

#### 测试查询1：前端框架
- **查询**：如何开发 React 组件？
- **预期结果**：应该找到"React 组件开发"相关的内容
- **相似度阈值**：0.7

#### 测试查询2：Vue 相关
- **查询**：Vue.js 的组件系统
- **预期结果**：应该找到"Vue.js 框架介绍"相关的内容
- **相似度阈值**：0.7

#### 测试查询3：项目管理
- **查询**：项目管理的核心功能
- **预期结果**：应该找到"项目管理工具功能"和"任务管理"相关的内容
- **相似度阈值**：0.6

#### 测试查询4：数据库
- **查询**：数据库设计的规范化原则
- **预期结果**：应该找到"数据库设计原则"相关的内容
- **相似度阈值**：0.7

#### 测试查询5：API 设计
- **查询**：如何设计 RESTful API？
- **预期结果**：应该找到"RESTful API 设计"相关的内容
- **相似度阈值**：0.7

#### 测试查询6：同义词测试
- **查询**：前端 UI 框架
- **预期结果**：应该找到 React 和 Vue.js 相关的内容（即使没有直接提到"UI 框架"）
- **相似度阈值**：0.6

#### 测试查询7：语义理解
- **查询**：如何管理任务和项目？
- **预期结果**：应该找到项目管理相关的所有内容
- **相似度阈值**：0.6

### 步骤5：测试推荐系统

1. 在数据项列表中，选择一个数据项
2. 调用推荐 API（可以通过浏览器控制台或 Postman）
3. 查看推荐的相似内容

### 步骤6：测试相似度阈值

尝试不同的相似度阈值，观察搜索结果的变化：

- **阈值 0.9**：只返回非常相似的结果（可能没有结果）
- **阈值 0.7**：返回相似的结果（推荐）
- **阈值 0.5**：返回相关的结果（可能包含不太相关的内容）
- **阈值 0.3**：返回很多结果（可能包含不相关的内容）

## 🔍 测试验证点

### 1. 向量化功能
- ✅ 数据项可以成功向量化
- ✅ 向量化后显示"已向量化"标签
- ✅ 向量化计数正确

### 2. 语义搜索功能
- ✅ 可以搜索到相关的内容
- ✅ 搜索结果按相似度排序
- ✅ 相似度分数显示正确
- ✅ 支持自然语言查询
- ✅ 理解语义含义（不仅仅是关键词匹配）

### 3. 推荐系统功能
- ✅ 可以推荐相似的内容
- ✅ 推荐结果按相似度排序
- ✅ 不包含自身

### 4. 性能测试
- ✅ 搜索响应速度快（< 1秒）
- ✅ 向量化速度合理（< 5秒/数据项）
- ✅ 支持大量数据项

## 📊 测试数据统计

- **数据项数量**：6 个
- **向量块数量**：约 6-12 个（取决于内容长度）
- **搜索测试**：7 个查询
- **推荐测试**：每个数据项

## 🐛 常见问题

### Q1: 向量化失败
- 检查 API Key 是否正确
- 检查网络连接
- 查看错误日志

### Q2: 搜索无结果
- 降低相似度阈值
- 检查数据项是否已向量化
- 尝试不同的查询词

### Q3: 搜索结果不准确
- 调整相似度阈值
- 检查数据项内容质量
- 使用更准确的查询词

## 🎯 测试 checklist

- [ ] 配置 OpenAI API Key
- [ ] 添加测试数据（6 个数据项）
- [ ] 向量化所有数据项
- [ ] 测试语义搜索（7 个查询）
- [ ] 测试推荐系统
- [ ] 测试相似度阈值
- [ ] 验证搜索结果准确性
- [ ] 验证性能（响应速度）

## 📝 测试报告模板

```
测试日期：___________
测试人员：___________

配置信息：
- API Key: [已配置/未配置]
- 嵌入模型: ___________
- 相似度阈值: ___________
- 最大结果数: ___________

测试结果：
- 数据项数量: ___________
- 向量化成功率: ___________
- 搜索测试通过率: ___________
- 平均搜索响应时间: ___________

问题记录：
1. ___________
2. ___________

建议：
1. ___________
2. ___________
```

---

**祝测试顺利！** 🎉

