# 向量搜索功能使用指南

## 🎯 功能概述

基于 Supabase pgvector 扩展实现的向量搜索功能，支持：
- ✅ **语义搜索** - 基于向量相似性的智能搜索
- ✅ **推荐系统** - 基于相似性推荐相关内容
- ✅ **相似性匹配** - 找到语义相似的内容
- ✅ **AI 应用开发** - 支持 RAG（检索增强生成）

## 📋 前置要求

### 1. 配置 OpenAI API Key

**需要配置 OpenAI API Key**，用于：
- 生成文本的嵌入向量（embeddings）
- AI 聊天功能

**获取 API Key：**
1. 访问 [OpenAI Platform](https://platform.openai.com/api-keys)
2. 创建新的 API Key
3. 在模型配置页面输入 API Key

### 2. 数据库迁移

执行数据库迁移以创建向量搜索相关的表：

```bash
# 使用 PowerShell
Get-Content supabase/migrations/20240104000000_create_vector_search.sql | docker exec -i supabase_db_project-manager psql -U postgres -d postgres
```

或者使用 Supabase CLI：

```bash
supabase migration up
```

## 🚀 快速开始

### 步骤1：配置模型

1. 进入 **AI 助手 → 模型配置**
2. 选择 **OpenAI (推荐)**
3. 输入 **OpenAI API Key**
4. 选择 **聊天模型**（如 gpt-3.5-turbo）
5. 选择 **嵌入模型**（如 text-embedding-3-small）
6. 点击 **"保存配置"**

### 步骤2：添加数据项

1. 进入 **AI 助手 → 数据管理**
2. 点击 **"添加数据项"**
3. 填写：
   - **标题**：数据项标题
   - **内容**：要搜索的内容
   - **分类**：（可选）分类标签
   - **标签**：（可选）用逗号分隔的标签
4. 点击 **"确定"**

### 步骤3：向量化数据项

1. 在数据项列表中，点击 **"向量化"** 按钮
2. 系统会自动：
   - 将内容分块
   - 生成嵌入向量
   - 存储到数据库
3. 等待向量化完成

### 步骤4：语义搜索

1. 进入 **AI 助手 → 语义搜索**
2. 输入搜索查询（支持自然语言）
3. 调整参数：
   - **相似度阈值**：0-1，值越高要求越相似
   - **返回结果数**：1-50
4. 点击 **"搜索"**
5. 查看搜索结果，按相似度排序

## 📊 功能详解

### 1. 数据管理

**功能：**
- 添加数据项（标题和内容）
- 编辑数据项
- 删除数据项
- 向量化数据项

**使用场景：**
- 存储要搜索的内容
- 管理知识库
- 构建推荐系统数据源

### 2. 语义搜索

**功能：**
- 基于向量相似性搜索
- 支持自然语言查询
- 可调节相似度阈值
- 显示相似度分数

**优势：**
- 理解语义含义，不仅仅是关键词匹配
- 支持同义词和相似概念
- 快速响应

**使用场景：**
- 知识库搜索
- 内容推荐
- 相似内容查找
- 智能问答（RAG）

### 3. 推荐系统

**功能：**
- 基于向量相似性推荐
- 自动找到相似内容
- 可调节推荐数量

**使用场景：**
- 内容推荐
- 相关文章推荐
- 相似产品推荐

### 4. 相似性匹配

**功能：**
- 计算两个内容的相似度
- 找到最相似的内容
- 支持批量匹配

**使用场景：**
- 去重检测
- 内容分类
- 相似度分析

## 🔧 技术实现

### 数据库结构

**vector_items 表：**
- 存储数据项（标题、内容、分类、标签）
- 用户隔离（RLS）

**vector_item_embeddings 表：**
- 存储向量嵌入
- 支持分块存储
- 向量索引（HNSW 或 ivfflat）

### 向量生成

- 使用 OpenAI embeddings API
- 支持批量生成
- 自动分块处理

### 向量搜索

- 使用 PostgreSQL pgvector 扩展
- 余弦相似度计算
- 高效的向量索引

### 数据库函数

**search_vector_items：**
- 向量相似性搜索
- 支持用户过滤
- 支持分类过滤

**recommend_similar_items：**
- 推荐相似项
- 基于向量相似性
- 排除自身

## 📈 性能优化

### 1. 向量索引

- **HNSW 索引**：如果 pgvector 版本支持，使用 HNSW（性能更好）
- **ivfflat 索引**：如果不支持 HNSW，使用 ivfflat（兼容性好）

### 2. 分块策略

- 默认分块大小：500 字符
- 可调节分块大小
- 支持重叠分块（未来功能）

### 3. 批量处理

- 批量生成嵌入向量
- 批量插入数据库
- 减少 API 调用次数

## 💡 使用技巧

### 1. 相似度阈值

- **0.7-0.8**：严格匹配，只返回非常相似的结果
- **0.5-0.7**：中等匹配，返回相关结果
- **0.3-0.5**：宽松匹配，返回可能相关的结果

### 2. 内容质量

- 内容越详细，搜索结果越准确
- 使用清晰的标题和描述
- 添加分类和标签便于过滤

### 3. 分块大小

- **小分块（200-300）**：精确匹配，适合短文本
- **中等分块（500-800）**：平衡精确度和上下文
- **大分块（1000+）**：保留更多上下文，适合长文本

### 4. 嵌入模型选择

- **text-embedding-3-small**：性价比高，适合大多数场景
- **text-embedding-3-large**：更准确，但更昂贵
- **text-embedding-ada-002**：旧版，不推荐

## 🔒 安全说明

### 1. 数据隐私

- 所有数据存储在 Supabase 数据库中
- 用户只能访问自己的数据（RLS）
- API Key 安全存储

### 2. API 使用

- OpenAI API 按使用量收费
- 建议设置使用限额
- 监控 API 使用情况

### 3. 数据备份

- 定期备份数据库
- 导出向量数据
- 保护 API Key

## ❓ 常见问题

### Q1: 向量化失败怎么办？

A: 检查：
1. API Key 是否正确
2. 网络连接是否正常
3. OpenAI API 是否可用
4. 查看错误日志

### Q2: 搜索结果不准确？

A: 尝试：
1. 降低相似度阈值
2. 增加数据项内容
3. 使用更准确的嵌入模型
4. 调整分块大小

### Q3: 向量化很慢？

A: 优化：
1. 减少分块大小
2. 使用批量处理
3. 检查网络速度
4. 使用更快的嵌入模型

### Q4: 如何提高搜索性能？

A: 优化：
1. 使用 HNSW 索引
2. 减少向量维度（如果可能）
3. 优化查询参数
4. 使用缓存（未来功能）

## 📚 相关资源

- [Supabase pgvector 文档](https://supabase.com/docs/guides/ai/vector-columns)
- [OpenAI Embeddings 文档](https://platform.openai.com/docs/guides/embeddings)
- [向量搜索最佳实践](https://www.pinecone.io/learn/vector-search-best-practices/)

## 🆘 获取帮助

如果遇到问题：
1. 查看错误日志
2. 检查 API Key 配置
3. 验证数据库迁移
4. 联系技术支持

---

**祝您使用愉快！** 🎉

