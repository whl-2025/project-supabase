# 文件管理功能设置指南

## 功能概述

文件管理功能允许用户：
- 上传文档到项目中
- 按照 `user_id/project_id/filename` 的层次结构组织文件
- 查看上传进度
- 下载文件
- 删除文件
- 文件大小限制：50MB
- 支持多种文件类型

## 设置步骤

### 步骤 1：创建 Storage Bucket

#### 方法 A：使用 Supabase Dashboard（推荐）

1. 打开 Supabase Dashboard
   - 本地：http://localhost:54323
   - 或者你的 Supabase 项目 URL

2. 进入 Storage 页面

3. 点击 "New bucket"

4. 配置 Bucket：
   - **Name**: `documents`
   - **Public**: ❌ 不勾选（私有 bucket）
   - **File size limit**: `52428800` (50MB)
   - **Allowed MIME types**: 留空（在代码中控制）

5. 点击 "Create bucket"

#### 方法 B：使用 Supabase CLI

```bash
# 创建 bucket
supabase storage create documents --public=false
```

### 步骤 2：执行数据库迁移

```bash
# 执行迁移文件
supabase db push

# 或者手动执行
psql -f supabase/migrations/20240120000000_setup_storage_policies.sql
```

### 步骤 3：验证设置

#### 检查 Bucket

```sql
-- 在 SQL Editor 中运行
SELECT * FROM storage.buckets WHERE name = 'documents';
```

应该返回一条记录。

#### 检查策略

```sql
-- 查看 Storage 策略
SELECT * FROM pg_policies 
WHERE schemaname = 'storage' 
AND tablename = 'objects';
```

应该看到 4 条策略：
- Users can upload files to their own directory
- Users can view their own files
- Users can update their own files
- Users can delete their own files

### 步骤 4：测试功能

1. 访问 http://localhost:3000/files

2. 点击"上传文件"按钮

3. 选择项目

4. 拖拽或选择文件

5. 点击"开始上传"

6. 查看上传进度

7. 上传完成后，文件应该出现在列表中

8. 测试下载和删除功能

## 目录结构

### Storage Bucket 结构

```
documents/                    （Bucket 名称）
├── user_123/                 （用户 ID）
│   ├── project_456/          （项目 ID）
│   │   ├── 需求文档.pdf
│   │   ├── 设计稿.docx
│   │   └── 会议记录.txt
│   └── project_789/
│       └── 技术方案.pdf
└── user_234/
    └── project_456/
        └── 测试报告.pdf
```

### 数据库记录

```sql
-- files 表结构
CREATE TABLE files (
  id UUID PRIMARY KEY,
  user_id UUID,              -- 用户 ID
  project_id UUID,           -- 项目 ID
  name TEXT,                 -- 文件名
  size BIGINT,               -- 文件大小（字节）
  mime_type TEXT,            -- MIME 类型
  storage_path TEXT,         -- 存储路径
  url TEXT,                  -- 公共 URL
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
);
```

## 支持的文件类型

### 文档类型
- PDF (`.pdf`)
- Word (`.doc`, `.docx`)
- Excel (`.xls`, `.xlsx`)
- PowerPoint (`.ppt`, `.pptx`)
- 文本 (`.txt`, `.md`)

### 图片类型
- JPEG (`.jpg`, `.jpeg`)
- PNG (`.png`)
- GIF (`.gif`)
- WebP (`.webp`)
- SVG (`.svg`)

### 压缩文件
- ZIP (`.zip`)
- RAR (`.rar`)
- 7Z (`.7z`)

## 文件大小限制

- **单个文件最大**: 50MB
- **总存储空间**: 取决于 Supabase 计划

## 安全策略

### Row Level Security (RLS)

所有策略都基于 `auth.uid()`，确保：
- 用户只能上传到自己的目录
- 用户只能查看自己的文件
- 用户只能删除自己的文件

### 存储路径验证

```typescript
// 路径格式：user_id/project_id/filename
const storagePath = `${userId}/${projectId}/${filename}`;
```

Storage 策略会验证路径的第一部分是否匹配当前用户 ID。

## 常见问题

### Q1: 上传失败，提示 "new row violates row-level security policy"

**原因**: Storage 策略未正确设置

**解决方案**:
1. 检查 bucket 是否创建
2. 执行迁移文件设置策略
3. 确认用户已登录

### Q2: 文件上传成功，但列表中看不到

**原因**: 数据库记录插入失败

**解决方案**:
1. 检查 `files` 表的 RLS 策略
2. 确认 `user_id` 字段已添加
3. 查看浏览器控制台错误信息

### Q3: 下载失败

**原因**: Storage 策略不允许读取

**解决方案**:
1. 检查 "Users can view their own files" 策略
2. 确认 `storage_path` 正确

### Q4: 如何修改文件大小限制？

**方法 1**: 在 Dashboard 中修改
1. Storage → documents bucket → Settings
2. 修改 "File size limit"

**方法 2**: 修改代码
```typescript
// app/files/page.tsx
const FILE_CONFIG = {
  maxSize: 100 * 1024 * 1024, // 改为 100MB
  // ...
};
```

### Q5: 如何添加新的文件类型？

修改 `FILE_CONFIG`:

```typescript
const FILE_CONFIG = {
  allowedTypes: [
    // 添加新的 MIME 类型
    'application/json',
    'text/csv',
    // ...
  ],
  allowedExtensions: [
    // 添加新的扩展名
    '.json',
    '.csv',
    // ...
  ]
};
```

## 性能优化

### 1. 使用 CDN

Supabase Storage 自动使用 CDN，无需额外配置。

### 2. 图片优化

对于图片文件，可以使用 Supabase 的图片转换功能：

```typescript
const { data } = supabase.storage
  .from('documents')
  .getPublicUrl(path, {
    transform: {
      width: 800,
      height: 600,
      resize: 'contain'
    }
  });
```

### 3. 分页加载

文件列表已实现分页，默认每页 10 条。

## 监控和维护

### 查看存储使用情况

```sql
-- 查看总文件数
SELECT COUNT(*) FROM files;

-- 查看总存储大小
SELECT pg_size_pretty(SUM(size)::bigint) as total_size
FROM files;

-- 查看每个用户的存储使用
SELECT 
  user_id,
  COUNT(*) as file_count,
  pg_size_pretty(SUM(size)::bigint) as total_size
FROM files
GROUP BY user_id;
```

### 清理孤立文件

```sql
-- 查找数据库中有记录但 Storage 中不存在的文件
-- 需要手动检查和清理
```

## 备份策略

### 自动备份

Supabase 会自动备份数据库，但 Storage 文件需要单独备份。

### 手动备份

使用 Supabase CLI：

```bash
# 下载所有文件
supabase storage download documents --recursive
```

## 扩展功能

### 1. 文件预览

可以添加文件预览功能：
- PDF: 使用 `react-pdf`
- 图片: 直接显示
- 文档: 转换为 PDF 后预览

### 2. 文件分享

添加分享链接功能：
- 生成临时访问链接
- 设置过期时间
- 记录访问日志

### 3. 版本控制

实现文件版本管理：
- 保存文件历史版本
- 支持回滚
- 对比版本差异

### 4. 批量操作

添加批量功能：
- 批量下载（打包为 ZIP）
- 批量删除
- 批量移动

## 总结

文件管理功能已完整实现：
- ✅ 文件上传（带进度条）
- ✅ 文件下载
- ✅ 文件删除
- ✅ 文件大小限制（50MB）
- ✅ 文件类型限制
- ✅ 层次化存储结构
- ✅ 安全策略（RLS）
- ✅ 用户隔离

按照本指南设置后，即可正常使用文件管理功能！
