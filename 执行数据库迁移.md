# 执行数据库迁移指南

## 问题说明

如果遇到错误：`Could not find the table 'public.ai_conversations' in the schema cache`，说明数据库迁移文件没有执行。

## 解决方法

### 方法一：使用 PowerShell 脚本（Windows 推荐）

1. 确保 PostgreSQL 客户端已安装（psql 命令可用）
2. 在项目根目录运行：

```powershell
.\init-supabase.ps1
```

### 方法二：手动执行迁移（Windows PowerShell）

```powershell
# 设置环境变量
$env:PGPASSWORD = "your-super-secret-jwt-token-with-at-least-32-characters-long"

# 执行项目表迁移
psql -h localhost -p 54322 -U postgres -d postgres -f "supabase/migrations/20240101000000_init_projects.sql"

# 执行 AI 功能迁移（重要！）
psql -h localhost -p 54322 -U postgres -d postgres -f "supabase/migrations/20240102000000_init_ai_features.sql"
```

### 方法三：使用 Docker 执行迁移

如果使用 Docker Compose：

```powershell
# 执行项目表迁移
docker exec -i supabase-postgres psql -U postgres -d postgres < supabase/migrations/20240101000000_init_projects.sql

# 执行 AI 功能迁移（重要！）
docker exec -i supabase-postgres psql -U postgres -d postgres < supabase/migrations/20240102000000_init_ai_features.sql
```

### 方法四：使用 Supabase CLI

如果使用 Supabase CLI：

```bash
# 重置数据库并执行所有迁移
supabase db reset

# 或者只执行迁移
supabase migration up
```

## 验证迁移是否成功

执行迁移后，可以连接数据库验证表是否创建：

```powershell
$env:PGPASSWORD = "your-super-secret-jwt-token-with-at-least-32-characters-long"
psql -h localhost -p 54322 -U postgres -d postgres -c "\dt"
```

应该看到以下表：
- projects
- ai_settings
- ai_conversations
- ai_messages
- documents
- document_embeddings

## 刷新 PostgREST Schema Cache

执行迁移后，如果 PostgREST 仍然找不到表，需要重启 PostgREST 服务以刷新 schema cache：

### 如果使用 Docker Compose：

```powershell
docker restart supabase-rest
```

### 如果使用 Supabase CLI：

```bash
supabase stop
supabase start
```

## 注意事项

1. **必须执行两个迁移文件**：
   - `20240101000000_init_projects.sql` - 创建项目表
   - `20240102000000_init_ai_features.sql` - 创建 AI 相关表（ai_conversations, ai_messages 等）

2. **迁移顺序很重要**：必须按照文件名的顺序执行

3. **如果表已存在**：迁移文件使用了 `CREATE TABLE IF NOT EXISTS`，可以安全地重复执行

4. **重启服务**：执行迁移后，建议重启 Supabase 服务以刷新 schema cache

