# 前端 Supabase 客户端配置指南

## ✅ 已配置的代码

前端 Supabase 客户端已经在 `utils/supabase/client.ts` 中配置好了：

```typescript
import { createBrowserClient } from "@supabase/ssr";

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
```

你只需要配置环境变量即可！

---

## 📝 配置步骤

### 1. 创建 `.env.local` 文件

在项目根目录创建 `.env.local` 文件（如果还没有的话）。

### 2. 根据你的启动方式配置

#### 方式一：使用 Supabase CLI（推荐）

如果你使用 `supabase start` 启动，配置如下：

```env
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<从 supabase start 输出中复制>
```

**获取 ANON_KEY 的方法：**

运行 `supabase start` 后，会显示类似以下输出：

```
API URL: http://localhost:54321
DB URL: postgresql://postgres:postgres@localhost:54322/postgres
Studio URL: http://localhost:54323
anon key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

复制 `anon key` 的值到 `.env.local`。

或者运行以下命令查看：

```bash
supabase status
```

#### 方式二：使用 Docker Compose

如果你使用 `docker-compose up` 启动，配置如下：

```env
NEXT_PUBLIC_SUPABASE_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
```

**注意：** 根据你当前的容器状态，Kong 网关运行在 `54321` 端口，所以应该使用：

```env
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
```

#### 方式三：使用云端 Supabase

如果你使用云端 Supabase 项目，配置如下：

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-from-dashboard
```

从 **Supabase Dashboard** → **Settings** → **API** 获取这些值。

---

## 🔍 如何确认当前配置

### 检查端口使用情况

运行以下命令查看当前运行的容器和端口：

```bash
docker ps --format "table {{.Names}}\t{{.Ports}}" | findstr supabase
```

### 检查 Kong 网关端口

Kong 网关通常运行在：
- **8000** (Docker Compose)
- **54321** (Supabase CLI)

---

## 📋 完整的 `.env.local` 示例

根据你当前的容器状态（Kong 在 54321 端口），配置应该是：

```env
# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
```

---

## ✅ 配置完成后

1. **保存 `.env.local` 文件**

2. **重启 Next.js 开发服务器**（如果正在运行）

   ```bash
   # 停止当前服务器 (Ctrl+C)
   # 然后重新启动
   npm run dev
   ```

3. **验证配置**

   在浏览器中访问应用，如果配置正确，应该能够：
   - 访问登录/注册页面
   - 创建账户
   - 登录后查看项目列表

---

## 🐛 常见问题

### 问题 1：连接错误

**错误信息：** `Failed to fetch` 或 `Network error`

**解决方案：**
- 确保 Docker 容器正在运行：`docker ps`
- 检查端口是否正确（Kong 应该在 8000 或 54321）
- 确保 `.env.local` 文件中的 URL 正确

### 问题 2：环境变量未生效

**解决方案：**
- 确保文件名为 `.env.local`（不是 `.env.local.example`）
- 重启 Next.js 开发服务器
- 检查环境变量名称是否正确（必须以 `NEXT_PUBLIC_` 开头）

### 问题 3：认证失败

**解决方案：**
- 检查 `ANON_KEY` 是否正确
- 确保数据库迁移已执行
- 检查 GoTrue 服务是否运行：`docker ps | findstr auth`

---

## 📚 相关文档

- [SETUP_LOCAL.md](./SETUP_LOCAL.md) - 本地开发环境配置
- [QUICK_START.md](./QUICK_START.md) - 快速启动指南
- [README.md](./README.md) - 项目说明

---

## 💡 提示

- 环境变量必须以 `NEXT_PUBLIC_` 开头才能在浏览器中访问
- `.env.local` 文件不会被提交到 Git（已在 `.gitignore` 中）
- 修改环境变量后需要重启 Next.js 开发服务器才能生效

